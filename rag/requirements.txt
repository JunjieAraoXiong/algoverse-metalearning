# Requirements for myRAG system
# Install with: pip install -r requirements.txt

# Core dependencies
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0

# HuggingFace datasets
datasets>=2.14.0

# LangChain and vector stores
langchain>=0.1.0
langchain-chroma>=0.1.0
langchain-together>=0.1.0
langchain-openai>=0.0.5
langchain-text-splitters>=0.0.1
langchain-community>=0.0.1
langchain-core>=0.1.0
chromadb>=0.4.0
rank_bm25>=0.2.2  # Required by BM25Retriever (not auto-installed by langchain)

# API clients
openai>=1.0.0
anthropic>=0.40.0
google-generativeai>=0.8.0
cohere>=5.0.0  # SOTA reranking (+28% NDCG improvement)

# Environment management
python-dotenv>=1.0.0

# Document processing (Docling for PDF ingestion)
docling>=2.0.0  # Best-in-class table extraction (97.9% accuracy)
pillow>=10.0.0  # Image processing

# Reranking and local embeddings
sentence-transformers>=2.2.0
langchain-huggingface>=0.0.3

# HTML parsing for table conversion
beautifulsoup4>=4.12.0

# Inference Server
vllm>=0.6.3
